# Packet C: Product UX Auditor

Objective:
Audit relevant iOS advanced execution screens for pseudo settings, static copy, empty states, and expected user flows.

Files / sources:
- SubAgentsView, SubAgentRoleView
- CouncilView, CouncilSettingsView, CouncilChatRuntimeView
- ExecutionSettingsView, RuntimeEnvironmentView
- PermissionsApprovalView

Do:
- Identify where UI currently behaves like documentation rather than a usable feature.
- Recommend minimal UI changes that preserve current visual design.

Do not:
- Edit files.

Expected output:
UI closure checklist with specific screen-level recommendations.
