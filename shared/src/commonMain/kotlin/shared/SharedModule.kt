package shared

/**
 * Umbrella module that re-exports all KMP shared modules.
 * Swift code imports Shared framework and gets access to all types transitively.
 */
object SharedModule {
    const val VERSION = "1.0.0"
}
