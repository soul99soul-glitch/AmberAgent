# 今日看板 — 开发交接文档

> 日期: 2026-05-11
> 当前进度: Phase 1-4 完成, Phase 5 UI 层待实现

---

## 一、项目位置

```
/Users/arquiel/Downloads/AI/rikkashit/rikkahub/
```

今日看板代码在 `app/src/main/java/me/rerere/rikkahub/data/agent/board/` 下。

## 二、已完成 (Phase 1-4, Oracle Reviewed)

### Phase 1: 数据基础
- 4 Entity / 4 DAO / Migration_22_23 (DB 22->23)
- BoardRepository / TodayBoardSetting (在 AgentRuntimeSetting 下)

### Phase 2: 采集层
- 6 Collectors (通知/日历/飞书消息/飞书文档/聊天记录/时间锚点)
- SignalAggregator (Mutex去重 + 权重打分 + 增量触发debounce)
- AmberNotificationListenerService 扩展 (Listener + addIfAbsent)

### Phase 3: 推理层
- BoardPrompt (密度感知信号上限, focus rules, 中文)
- BoardOutputParser (容错JSON解析)
- BoardOutputValidator (幻觉过滤 + 密度cap + 跨category去重)
- BoardAgent (调模型->解析->验证->持久化, retry带纠偏)
- 幂等PK: SHA256(boardDate|source_ref|category)

### Phase 4: 调度层
- BoardScheduler: anchor + incremental + manual
- BoardWorker: collect->filter->agent.run->markProcessed->prune, retry cap=3
- BoardNotifier + RikkaHubApp 启动钩子 + foreground compensation

---

## 三、待实现 (Phase 5-6)

### Phase 5: UI 层

**5.1 路由 + 入口**
- RouteActivity.kt: 加 `data object TodayBoard : Screen`
- entry 注册: `entry<Screen.TodayBoard> { BoardPage(...) }`
- ChatDrawer.kt DrawerActions: Search和History之间加DrawerNavRow (条件: todayBoard.enabled)
- 处理 BoardNotifier.EXTRA_OPEN_TODAY_BOARD intent extra

**5.2 看板页面**
- 新建 ui/pages/board/ 目录
- BoardViewModel: 观察 BoardRepository.observeItems(todayBoardDate), 暴露 items Flow + summary
- BoardPage: Scaffold + TopAppBar + 双Tab (分区/紧急度)
- Tab A 分区式: 按category分组 (action/attention/info), 每组Section header
- Tab B 紧急度: Top3置顶 + 时间轴列表 (按signalTime排序)
- 用 NotionListRow 作为条目基础组件
- 空态: 功能未启用 / 无数据

**5.3 条目交互**
- 点击展开: reason + suggestion + sourceContent
- 完成: markItemCompleted -> 灰色+删除线
- 忽略: 左滑或长按 -> markItemDismissed
- 聊一下: 创建新Conversation, 注入system prompt, 跳ChatPage
- 下拉刷新: BoardScheduler.runOnce()
- 导出Markdown / 发到Chat

**5.4 设置页**
- SettingExperimentalPage: 加今日看板入口
- SettingTodayBoardPage:  | 结论 |
|------|------|
| 看板入口 | 侧边栏固定位置，点击跳全屏页 |
| 看板形态 | 双Tab: 分区式 + 紧急度 |
| 触发策略 | 定时(08/12/18) + 增量(>=5信号) + 手动/前台补偿 |
| Agent推理 | 非流式，全量信号一次性丢给LLM |
| 失败兜底 | 重试1次(带纠偏) + 保留旧版本 |
| 完成态清除 | 凌晨4:00 |
| 条目PK | SHA256(boardDate|source_ref|category), REPLACE幂等 |
| 反馈学习 | Level1权重规则 + Level2 Prompt记忆(v1.1) |
| 输出语言 | 固定中文 |
| 功能开关 | 实验性功能下，关闭时入口隐藏 |
| 看板密度 | compact(5/3/2) / standard(8/5/3) / rich(12/8/5) |
| 聊一下对话 | 不做特殊标记 |
| 分享 | MVP: 导出Markdown + 发到Chat |

---

## 五、项目约定

- 4空格缩进, 120列
- Koin DI (KoinComponent + get<>())
- Room DB + 手写Migration
- Compose UI + Material3
- 不需要i18n (直接写中文)
- Screen路由: sealed interface Screen : NavKey + @Serializable data object
- UI组件: 用NotionListRow/NotionSlider (在ui/components/ui/)
- workspaceColors() 获取主题色

---

## 六、关键参考文件

- RouteActivity.kt: Screen sealed interface (~line 652), entry注册 (~line 346)
- ChatDrawer.kt: DrawerActions, DrawerNavRow组件
- NotionListRow.kt: 列表行UI规范
- SettingExperimentalPage.kt: 实验性功能入口
- BoardRepository.kt: 数据访问API (observeItems, markItemCompleted等)
- BoardScheduler.kt: runOnce() 用于手动刷新
- MemoryDreamPlanner.kt: LLM调用模式参考
- FeishuDocumeshit/今日看板-产品方案-v0.1.md`
