  text=Link Edge Cases
  text=This file tests link syntax that is likely to trip up parsers or renderers.
  text=Parentheses in URLs
  text=Links whose URLs contain unbalanced parentheses need careful handling. CommonMark allows balanced parentheses without escaping:
  text=MDN Web Docs
  text=Wikipedia: Kotlin (programming language)
  text=An URL with an escaped closing paren: example
  text=Spaces in URLs
  text=Spaces in link destinations must be encoded as %20 or the link must use angle brackets:
  text=Search with spaces
  text=Empty Link Text
  text=The spec allows an empty link text: . Renderers vary on whether they produce a visible link.
  text=Empty URL
  text=A link with an empty destination: empty URL — this is technically valid and should produce a link with href="".
  text=Link Immediately After Emphasis
  text=italiclink — no space between emphasis close and link open.
  text=boldanother link — same with bold.
  text=Link Followed Immediately by Punctuation
  text=Visit example.com. The period is not part of the URL.
  text=See the docs, then restart.
  text=Link at end of sentence?
  text=Angle-Bracket Link Destinations
  text=The angle-bracket form allows spaces and most special characters:
  text=weird URL
  text=Nested Brackets in Link Text
  text=Link text can contain [ and ] if they are balanced: nested brackets — this is uncommon but must not crash the parser.
  text=Reference Link—Undefined Reference
  text=A reference link with no matching definition: [this reference][undefined-ref] — CommonMark requires rendering this as literal text [this reference][undefined-ref].
  text=Link with Fragment Only
  text=Jump to heading — a fragment-only URL is valid.
  text=Protocol-Relative URL
  text=Protocol-relative — starts with // and inherits the page protocol.
